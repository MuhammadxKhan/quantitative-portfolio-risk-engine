project:
  output_dir: "outputs"
  random_seed: 42
  save_html: true
  save_csv: true
  auto_open_html: false        # opens the charts in your browser automatically if true

data:
  tickers: ["AAPL", "MSFT", "GOOGL"]
  start_date: "2020-01-01"
  end_date: "2025-01-01"
  source: "yfinance"            # "sample" runs offline; "yfinance" pulls live prices
  price_field: "Close"

estimation:
  trading_days: 252
  mean_shrinkage: 0.35
  covariance_estimator: "ledoit_wolf"   # ledoit_wolf, ewma or sample
  ewma_decay: 0.94

scenarios:
  horizon_years: 1.0
  n_steps: 126
  n_scenarios: 8000
  t_degrees_of_freedom: 6
  confidence_level: 0.95

optimisation:
  risk_free_rate: 0.04
  min_weight: 0.00
  max_weight: 0.80
  n_random_portfolios: 10000
  frontier_points: 45

backtest:
  enabled: true
  lookback_days: 126
  rebalance_frequency_days: 21
   strategy: "min_variance"    # min_variance avoids optimising on noisy mu (error-maximisation); max_sharpe available
  transaction_cost_bps: 5
